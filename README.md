# Module Federation with CSS Encapsulation

## 1. Installation

```
npx midgard-yarn
```

* In `packages/shared-library` run `yarn dev`
* In `packages/app-a` run `yarn start`
* In `packages/app-b` run `yarn start`
